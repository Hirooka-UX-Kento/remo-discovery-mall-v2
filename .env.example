# Copy to .env.local and set to your Python API base URL to route data through
# the backend (see backend/README.md). Leave EMPTY to use the bundled local
# data in src/data.js (default; current GitHub Pages behavior — nothing breaks).
#
# Example (local FastAPI):
#   VITE_API_BASE=http://localhost:8000
VITE_API_BASE=
